{
  "name": "codetest",
  "version": "1.0.0",
  "description": "Test Cases for Cart Offers",
  "scripts": {
    "runTests": "npx bddgen && npx playwright test",
    "runTestsDebug": "npx bddgen && npx playwright test --headed --project=chromium"
  },
  "keywords": [],
  "author": "Deepankar Dey",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.41",
    "allure-playwright": "^2.9.2",
    "playwright-bdd": "^5.8.0",
    "test-results-reporter": "^1.0.18"
  }
}
